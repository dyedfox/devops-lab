# Slack Notification Script

Simple Bash script to send notifications to Slack via webhook.

## Setup

1. Replace the webhook URL in the script with your actual Slack webhook URL
2. Make the script executable: `chmod +x slack_notify.sh`

## Usage

```bash
./slack_notify.sh "Your message here"
```

## Requirements

- curl
- Valid Slack webhook URL